#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - prints to the stdout
 *
 * Return: 0 if successful
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
	printf("%d", i);
	}
	putchar('\n');
	return (0);
}

#include "main.h"
/**
 * _atoi - function that convert string to integer
 * @s: assigned integer
 * Return: integer
 */
int _atoi(char *s)
{
	int i;
	unsigned int a = 0;

	if (n == 0)
	{
	return (a);
	}
}
